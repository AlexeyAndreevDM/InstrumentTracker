# 📦 Сборка InstrumentTracker

Система поддерживает сборку для **Windows**, **macOS** и **Linux**.

## Быстрый старт

### Windows
```cmd
pyinstaller InstrumentTracker_Windows.spec -y
```
📖 Подробная инструкция: [BUILD_WINDOWS.md](BUILD_WINDOWS.md)

### macOS
```bash
pyinstaller InstrumentTracker.spec -y
```
📖 Подробная инструкция: [BUILD_MACOS.md](BUILD_MACOS.md)

### Linux
```bash
pyinstaller InstrumentTracker_Linux.spec -y
```

## Где хранятся данные

| Платформа | Путь к базе данных |
|-----------|-------------------|
| **Windows** | `%APPDATA%\InstrumentTracker\inventory.db` |
| **macOS** | `~/Library/Application Support/InstrumentTracker/inventory.db` |
| **Linux** | `~/.local/share/InstrumentTracker/inventory.db` |

## Общие требования

- Python 3.11+
- PyQt6
- PyInstaller
- openpyxl (для импорта Excel)
- pillow (для работы с изображениями)

## Структура сборки

```
InstrumentTracker/
├── main.py                          # Главный файл приложения
├── database/                        # Модули работы с БД
│   ├── db_manager.py               # Менеджер базы данных
│   └── db_core.py                  # Создание таблиц
├── views/                          # Диалоги и UI
├── pictures/                       # Иконки и изображения
├── inventory.db                    # База данных (для копирования в bundle)
├── InstrumentTracker.spec         # Spec для macOS
├── InstrumentTracker_Windows.spec # Spec для Windows
└── requirements.txt               # Зависимости Python
```

## Кроссплатформенность

Код автоматически определяет платформу и настраивает пути:

- **Режим разработки**: `inventory.db` в текущей директории
- **Frozen режим** (после сборки):
  - Windows: БД в `%APPDATA%\InstrumentTracker\`
  - macOS: БД в `~/Library/Application Support/InstrumentTracker/`
  - Linux: БД в `~/.local/share/InstrumentTracker/`

## Горячие клавиши

Система использует универсальные комбинации:
- Windows/Linux: `Ctrl + <key>`
- macOS: автоматически заменяется на `⌘ + <key>`

PyQt6 автоматически обрабатывает это преобразование.

## Тестирование сборки

После сборки проверьте:

1. ✅ Приложение запускается
2. ✅ База данных создается автоматически
3. ✅ Тестовые данные загружаются при первом запуске
4. ✅ Все функции работают (вход, операции, отчеты)
5. ✅ Горячие клавиши работают
6. ✅ Темы переключаются
7. ✅ Иконки отображаются

## Размер приложения

Приблизительные размеры после сборки:

- **Windows**: ~150-200 MB
- **macOS**: ~150-200 MB  
- **Linux**: ~120-180 MB

Размер зависит от включенных библиотек Qt и других зависимостей.

## Дополнительная документация

- 📘 [Руководство пользователя](md%20files/AUTHORIZATION_GUIDE.md)
- 📋 [Примеры использования](md%20files/AUTHORIZATION_EXAMPLES.md)
- ⌨️ [Горячие клавиши](SHORTCUTS.md)
- 📧 [Настройка email](EMAIL_SETUP.md)

## Поддержка

При проблемах со сборкой:
1. Проверьте версию Python: `python --version` (должна быть 3.11+)
2. Убедитесь что все зависимости установлены: `pip list`
3. Очистите кэш сборки: добавьте флаг `--clean` к команде PyInstaller
4. Проверьте логи сборки на наличие ошибок

## Готово к сборке! 🚀

Выберите нужную платформу и следуйте соответствующей инструкции.
