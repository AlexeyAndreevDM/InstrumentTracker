# 📋 Примеры использования системы авторизации

## Сценарий 1: Администратор входит в систему

### Шаги:
1. Запускаем приложение
2. В окне входа на вкладке "🔑 Вход":
   - Имя пользователя: `admin`
   - Пароль: `admin`
3. Нажимаем "🔓 Вход"
4. Открывается главное окно с доступом ко всем функциям

### Результат:
- ✅ Доступна вкладка "📋 Каталог активов" для управления
- ✅ Доступна вкладка "📬 Запросы" для одобрения запросов пользователей
- ✅ Доступны кнопки добавления, редактирования, удаления активов
- ✅ Доступна кнопка импорта из Excel

---

## Сценарий 2: Пользователь создает новую учетную запись

### Шаги:
1. Запускаем приложение
2. Переходим на вкладку "✍️ Новый пользователь"
3. В комбобоксе "Выберите сотрудника":
   - Выбираем "Иванов Иван Иванович" из списка
   - Или печатаем свое ФИО вручную
4. Система автоматически генерирует:
   - Имя пользователя: `user1`
   - Пароль: `user1` (видно в поле пароля)
5. Нажимаем "✅ Создать аккаунт"
6. Видим сообщение об успехе
7. Система переводит нас на вкладку "🔑 Вход"
8. Вводим данные:
   - Имя пользователя: `user1`
   - Пароль: `user1`
9. Нажимаем "🔓 Вход"

### Результат:
- ✅ Новая учетная запись создана
- ✅ Вошли как пользователь `user1` с ролью `user`
- ✅ На панели управления видимся как "user1 (USER)"
- ✅ Не видны вкладки "📋 Каталог активов" и "📬 Запросы"
- ✅ Доступна только вкладка "🔄 Операции" с кнопкой "📝 Запросить актив"

---

## Сценарий 3: Пользователь запрашивает актив

### Предусловие:
- Пользователь вошел в систему как `user1`

### Шаги:
1. Переходим на вкладку "🔄 Операции"
2. Нажимаем кнопку "📝 Запросить актив"
3. В диалоге "Запросить актив":
   - Выбираем актив: "Шуруповерт Bosch (Электроинструмент) (доступно: 1)"
   - Дата плановой выдачи: Сегодня (автоматически)
   - Плановая дата возврата: по умолчанию через 7 дней
   - Примечание: "Нужно повесить полку в офисе"
4. Нажимаем "✅ Отправить запрос"
5. Видим сообщение: "✅ Запрос на выдачу актива отправлен администратору!"

### Результат:
- ✅ Запрос создан в таблице Asset_Requests со статусом "pending"
- ✅ Администратор может увидеть этот запрос во вкладке "📬 Запросы"

---

## Сценарий 4: Администратор одобряет запрос

### Предусловие:
- Администратор вошел в систему
- Пользователь `user1` создал запрос на актив

### Шаги:
1. Переходим на вкладку "📬 Запросы"
2. Видим запрос:
   - ID: 1
   - Актив: "Шуруповерт Bosch"
   - Модель: "GSR 120-LI"
   - Сотрудник: "Иванов Иван"
   - Дата запроса: Сегодня
   - Статус: "pending"
   - Примечание: "Нужно повесить полку в офисе"
3. Выбираем запрос в таблице
4. Нажимаем "✅ Одобрить"
5. Подтверждаем: "Одобрить запрос на выдачу 'Шуруповерт Bosch' для Иванов Иван?"
6. Видим сообщение: "✅ Запрос одобрен и актив выдан!"

### Результат:
- ✅ Статус запроса изменился на "approved"
- ✅ Создана запись в Usage_History с типом операции "выдача"
- ✅ Количество активов уменьшилось на 1 (с 1 на 0)
- ✅ Статус актива изменился на "Выдан"
- ✅ Информация об одобрении и одобрившем администраторе сохранена

---

## Сценарий 5: Администратор отклоняет запрос

### Предусловие:
- Администратор вошел в систему
- Был создан еще один запрос пользователем

### Шаги:
1. Переходим на вкладку "📬 Запросы"
2. Видим новый запрос
3. Выбираем запрос в таблице
4. Нажимаем "❌ Отклонить"
5. Подтверждаем: "Отклонить запрос на выдачу '[актив]'?"

### Результат:
- ✅ Статус запроса изменился на "rejected"
- ✅ Запись об отклонении сохранена
- ✅ Актив остался на складе в полном количестве
- ✅ Пользователь может увидеть отклоненный запрос в истории

---

## Сценарий 6: Пользователь возвращает актив

### Предусловие:
- Пользователю был выдан актив
- Пользователь вошел в систему

### Шаги:
1. Переходим на вкладку "🔄 Операции"
2. Нажимаем кнопку "📥 Вернуть актив"
3. В диалоге "Вернуть актив":
   - Выбираем актив, который брали: "Шуруповерт Bosch"
   - Вводим количество возвращаемых единиц: 1
   - Дата возврата: Сегодня (автоматически)
4. Нажимаем "✅ Вернуть"
5. Подтверждаем операцию

### Результат:
- ✅ Создана запись в Usage_History с типом операции "возврат"
- ✅ Количество активов увеличилось
- ✅ Статус актива изменился обратно на "Доступен"
- ✅ Дата фактического возврата зафиксирована

---

## Сценарий 7: Проверка разных прав доступа

### Для администратора (admin):
✅ Видны все вкладки:
- 🏠 Панель управления
- 📋 Каталог активов
- 🔄 Операции
- 📬 Запросы
- 📊 Отчеты

✅ На вкладке "🔄 Операции" видны кнопки:
- 📤 Выдать актив
- 📥 Вернуть актив
- 📝 Запросить актив
- 🔄 Обновить историю

### Для пользователя (user1):
✅ Видны вкладки:
- 🏠 Панель управления
- 🔄 Операции
- 📊 Отчеты

❌ Не видны вкладки:
- 📋 Каталог активов
- 📬 Запросы

✅ На вкладке "🔄 Операции" видны кнопки:
- 📥 Вернуть актив
- 📝 Запросить актив (видна, кнопка "📤 Выдать актив" скрыта)
- 🔄 Обновить историю

---

## Таблица соответствия: Учетные записи ↔️ Сотрудники

| Имя пользователя | ФИО сотрудника | Роль | Должность |
|---|---|---|---|
| admin | - | admin | - |
| user1 | Иванов Иван Иванович | user | Инженер |
| user2 | Петров Петр Петрович | user | Техник |
| user3 | Сидорова Мария Сергеевна | user | Кладовщик |

---

## 🔑 Интеграция с системой

### Для добавления нового администратора:

Администратор может создать новую учетную запись в вкладке "✍️ Новый пользователь", но по умолчанию она будет создана с ролью `user`. Чтобы изменить роль на `admin`, администратор должен отредактировать запись в БД вручную:

```sql
UPDATE Users SET role = 'admin' WHERE username = 'username';
```

### Для отключения пользователя:

```sql
UPDATE Users SET is_active = 0 WHERE username = 'user1';
```

### Для изменения пароля:

```sql
UPDATE Users SET password = 'new_password_hash' WHERE username = 'user1';
```

---

## ⚠️ Важные замечания

1. **Безопасность паролей:** Текущая реализация использует SHA-256 хеширование. Для production окружения рекомендуется использовать bcrypt или argon2.

2. **Аудит:** Все операции администратора (одобрение/отклонение запросов) логируются с сохранением информации об одобрившем администраторе и времени операции.

3. **Каскадное удаление:** При удалении пользователя автоматически удаляются связанные запросы.

4. **Восстановление доступа:** Используйте скрипт сброса БД для удаления всех пользователей и начала с нуля.
