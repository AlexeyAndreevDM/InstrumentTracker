# 🔐 Система авторизации и управления запросами на выдачу активов

## 🎯 Описание

Полностью реализована двухуровневая система авторизации с поддержкой двух типов пользователей:
- **Администраторы** - имеют полный доступ ко всем функциям системы
- **Обычные пользователи** - имеют ограниченный доступ с возможностью создания запросов на выдачу активов

---

## ✨ Ключевые особенности

### 🔑 Система авторизации
- Безопасное хранение паролей (SHA-256 хеширование)
- Двухуровневая авторизация (admin/user)
- Автоматическое генерирование учетных данных для пользователей
- Привязка учетных записей к сотрудникам

### 📝 Система запросов на выдачу
- Пользователи создают запросы на выдачу активов
- Администраторы могут одобрять или отклонять запросы
- Автоматическое обновление количества активов при одобрении
- Полный аудит операций

### 🎨 Интуитивный интерфейс
- Русскоязычный интерфейс
- Иконки для визуальной навигации
- Адаптивный дизайн
- Динамическое отображение элементов в зависимости от прав доступа

---

## 🚀 Быстрый старт

### Для администратора
1. Запустите приложение
2. На вкладке "🔑 Вход" введите:
   - Имя пользователя: `admin`
   - Пароль: `admin`
3. Нажмите "🔓 Вход"
4. Получите полный доступ ко всем функциям

### Для обычного пользователя
1. Запустите приложение
2. На вкладке "✍️ Новый пользователь":
   - Выберите свое ФИО из списка
   - Система автоматически создаст username и пароль
   - Нажмите "✅ Создать аккаунт"
3. На вкладке "🔑 Вход" введите созданные учетные данные
4. Получите доступ к ограниченному функционалу

---

## 📚 Полная документация

### 🔰 Для начинающих
📖 **[AUTHORIZATION_GUIDE.md](AUTHORIZATION_GUIDE.md)** - полное руководство пользователя

### 📋 Для примеров
📖 **[AUTHORIZATION_EXAMPLES.md](AUTHORIZATION_EXAMPLES.md)** - пошаговые примеры использования

### 🏗️ Для разработчиков
📖 **[AUTHORIZATION_ARCHITECTURE.md](AUTHORIZATION_ARCHITECTURE.md)** - архитектура системы и диаграммы

### ✅ Для проверки
📖 **[AUTHORIZATION_CHECKLIST.md](AUTHORIZATION_CHECKLIST.md)** - чек-лист функциональности

### 📊 Краткая сводка
📖 **[AUTHORIZATION_SUMMARY.md](AUTHORIZATION_SUMMARY.md)** - сводка по реализации

---

## 👤 Права доступа по ролям

| Функция | Admin | User |
|---------|-------|------|
| 🏠 Панель управления | ✅ | ✅ |
| 📋 Каталог активов | ✅ | ❌ |
| ➕ Добавить актив | ✅ | ❌ |
| ✏️ Редактировать актив | ✅ | ❌ |
| 🗑️ Удалить актив | ✅ | ❌ |
| 📥 Импорт из Excel | ✅ | ❌ |
| 📤 Выдать актив (прямое) | ✅ | ❌ |
| 📥 Вернуть актив | ✅ | ✅ |
| 📝 Запросить актив | ✅ | ✅ |
| 📬 Управление запросами | ✅ | ❌ |
| ✅ Одобрить запрос | ✅ | ❌ |
| ❌ Отклонить запрос | ✅ | ❌ |
| 📊 Просмотр отчетов | ✅ | ✅ |
| 💾 Экспорт данных | ✅ | ✅ |

---

## 🔄 Процесс работы с запросами

### Шаг 1: Пользователь создает запрос
```
Пользователь → "📝 Запросить актив" → Выбирает актив → Указывает дату → Отправляет
                                                                              ↓
                                                              Запрос со статусом 'pending'
```

### Шаг 2: Администратор просматривает запросы
```
Администратор → Вкладка "📬 Запросы" → Видит все запросы в таблице
                                                    ↓
                                         Может одобрить или отклонить
```

### Шаг 3: Администратор одобряет запрос
```
Администратор → Выбирает запрос → Нажимает "✅ Одобрить" → Подтверждает
                                                                  ↓
                                        Актив выдается пользователю
                                        История создана
                                        Статус изменен на 'approved'
```

### Шаг 4: Пользователь видит выданный актив
```
Пользователь видит в истории операций:
- Тип: '📤 Выдача'
- Актив: [название]
- Дата выдачи: [дата]
- Плановый возврат: [дата]

Может нажать "📥 Вернуть актив" для возврата
```

---

## 🗄️ Структура базы данных

### Таблица Users
```sql
CREATE TABLE Users (
    user_id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    employee_id INTEGER,
    role VARCHAR(20) DEFAULT 'user',
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES Employees(employee_id)
);
```

### Таблица Asset_Requests
```sql
CREATE TABLE Asset_Requests (
    request_id INTEGER PRIMARY KEY AUTOINCREMENT,
    asset_id INTEGER NOT NULL,
    employee_id INTEGER NOT NULL,
    request_date DATETIME NOT NULL,
    planned_return_date DATE,
    notes TEXT,
    status VARCHAR(20) DEFAULT 'pending',
    approved_by INTEGER,
    approved_at DATETIME,
    FOREIGN KEY (asset_id) REFERENCES Assets(asset_id),
    FOREIGN KEY (employee_id) REFERENCES Employees(employee_id),
    FOREIGN KEY (approved_by) REFERENCES Users(user_id)
);
```

---

## 📁 Структура проекта

```
InstrumentTracker/
├── main.py                              # Главное приложение (обновлено)
├── database/
│   ├── db_core.py                      # Инициализация БД (обновлено)
│   └── db_manager.py                   # Управление БД
├── views/
│   ├── login_dialog.py                 # Окно входа (НОВОЕ)
│   ├── request_dialog.py                # Диалог запроса (НОВОЕ)
│   ├── asset_dialog.py
│   ├── issue_dialog.py
│   ├── return_dialog.py
│   └── edit_asset_dialog.py
├── notification_manager.py
├── inventory.db                         # База данных
├── AUTHORIZATION_GUIDE.md               # Руководство пользователя (НОВОЕ)
├── AUTHORIZATION_EXAMPLES.md            # Примеры использования (НОВОЕ)
├── AUTHORIZATION_ARCHITECTURE.md        # Архитектура (НОВОЕ)
├── AUTHORIZATION_SUMMARY.md             # Сводка по функциям (НОВОЕ)
└── AUTHORIZATION_CHECKLIST.md           # Чек-лист функциональности (НОВОЕ)
```

---

## 🔧 Установка и запуск

### Требования
- Python 3.7+
- PyQt6
- SQLite3

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск приложения
```bash
python main.py
```

### Пересоздание базы данных
```bash
python reset_db.py
```

---

## 🛡️ Безопасность

### Хеширование паролей
- Используется SHA-256
- Пароли не хранятся в открытом виде
- Пароли не логируются и не отображаются

### Разделение прав доступа
- На уровне интерфейса (скрытие/показ элементов)
- На уровне БД (в future версиях)

### Аудит
- Сохраняется информация об одобрившем администраторе
- Сохраняется время одобрения/отклонения
- Все операции логируются в консоль

### Рекомендации для production
- Использовать bcrypt или argon2 вместо SHA-256
- Добавить таймауты сессии
- Реализовать двухфакторную аутентификацию
- Добавить логирование в файл
- Использовать защищенное соединение (HTTPS)

---

## 🐛 Известные ограничения

1. **Таймауты сессии:** Пользователь может быть в системе бесконечно долго
2. **Двухфакторная аутентификация:** Не реализована
3. **Восстановление пароля:** Нет автоматического восстановления
4. **Блокировка аккаунта:** При множественных неправильных попытках входа аккаунт не блокируется
5. **Логирование:** Логирует только в консоль, не в файл

---

## 🚦 Статус функциональности

| Компонент | Статус |
|-----------|--------|
| Окно входа | ✅ Завершено |
| Система авторизации | ✅ Завершено |
| Управление запросами | ✅ Завершено |
| Разделение прав | ✅ Завершено |
| Документация | ✅ Завершено |
| Тестирование | ✅ Завершено |

---

## 📞 Поддержка

При возникновении проблем обратитесь к администратору системы или разработчику.

### Часто встречаемые проблемы

**Проблема:** Забыл пароль
**Решение:** Обратитесь к администратору, он может пересоздать аккаунт

**Проблема:** Не могу создать аккаунт
**Решение:** Убедитесь, что вы выбрали свое ФИО из списка сотрудников

**Проблема:** Запрос не одобрен
**Решение:** Проверьте наличие активов на складе, администратор может отклонить запрос

---

## 📈 История версий

### v1.0 (8 декабря 2025)
- ✅ Реализована система авторизации
- ✅ Добавлены таблицы Users и Asset_Requests
- ✅ Реализовано управление запросами на выдачу
- ✅ Добавлено разделение прав доступа
- ✅ Полная документация

---

## 📄 Лицензия

Этот проект разработан для АО "КОНСИСТ-ОС"

---

**Версия:** 1.0  
**Дата:** 8 декабря 2025  
**Статус:** ✅ Готово к использованию
