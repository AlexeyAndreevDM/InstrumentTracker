# 🪟 Инструкция по сборке InstrumentTracker для Windows

## Требования

- **Windows 10/11** (64-bit)
- **Python 3.11+** установленный из [python.org](https://www.python.org/downloads/)
- **Git** (опционально, для клонирования репозитория)

## Шаг 1: Подготовка окружения

### 1.1 Клонируйте проект (если еще не сделали)

```cmd
git clone <URL_репозитория>
cd InstrumentTracker
```

### 1.2 Создайте виртуальное окружение

```cmd
python -m venv venv
```

### 1.3 Активируйте виртуальное окружение

```cmd
venv\Scripts\activate
```

## Шаг 2: Установка зависимостей

### 2.1 Обновите pip

```cmd
python -m pip install --upgrade pip
```

### 2.2 Установите необходимые пакеты

```cmd
pip install PyQt6 openpyxl pillow pyinstaller
```

Или если есть requirements.txt:

```cmd
pip install -r requirements.txt
pip install pyinstaller
```

## Шаг 3: Проверка работы приложения

Перед сборкой убедитесь, что приложение запускается:

```cmd
python main.py
```

Если всё работает - закройте приложение и переходите к сборке.

## Шаг 4: Сборка приложения

### 4.1 Запустите PyInstaller

**Важно:** Перед каждой новой сборкой рекомендуется очистить кэш:

```cmd
pyinstaller InstrumentTracker_Windows.spec --clean -y
```

Или более быстрая сборка без очистки (если ничего не менялось в зависимостях):

```cmd
pyinstaller InstrumentTracker_Windows.spec -y
```

Процесс сборки займет 2-5 минут. Вы увидите много INFO сообщений - это нормально.

### 4.2 Проверьте результат

После успешной сборки в папке `dist` появится папка `InstrumentTracker` с файлом `InstrumentTracker.exe`:

```
dist/
└── InstrumentTracker/
    ├── InstrumentTracker.exe  ← Главный исполняемый файл
    ├── pictures/
    │   ├── InstrumentTracker_icon.png
    │   └── InstrumentTracker_logo.png
    ├── inventory.db
    └── [множество DLL и библиотек]
```

## Шаг 5: Запуск приложения

### Вариант А: Запуск из папки dist

```cmd
cd dist\InstrumentTracker
InstrumentTracker.exe
```

### Вариант Б: Двойной клик в Проводнике

Откройте папку `dist\InstrumentTracker` в Проводнике Windows и дважды кликните по `InstrumentTracker.exe`.

## Шаг 6: Распространение приложения

### Создание ZIP архива

Чтобы передать приложение другим пользователям:

1. Скопируйте всю папку `dist\InstrumentTracker`
2. Создайте ZIP архив:
   - Правой кнопкой на папке → "Отправить" → "Сжатая ZIP-папка"
3. Передайте ZIP файл

### Важно для пользователей:

- ⚠️ **НЕ запускайте только .exe файл!** Нужна вся папка.
- Все файлы из папки `InstrumentTracker` должны находиться вместе
- База данных будет создана в `%APPDATA%\InstrumentTracker\inventory.db`
- При первом запуске автоматически создастся БД с тестовыми данными

## 🔧 Расположение данных

**База данных хранится в:**
```
C:\Users\<Имя_пользователя>\AppData\Roaming\InstrumentTracker\inventory.db
```

Чтобы найти эту папку:
1. Нажмите `Win + R`
2. Введите: `%APPDATA%\InstrumentTracker`
3. Нажмите Enter

## 📦 Создание установщика (опционально)

Для создания профессионального установщика можно использовать:

### Inno Setup (рекомендуется)

1. Скачайте Inno Setup: https://jrsoftware.org/isdl.php
2. Создайте скрипт установки (пример ниже)
3. Соберите .exe установщик

Пример скрипта Inno Setup (`installer.iss`):

```ini
[Setup]
AppName=InstrumentTracker
AppVersion=1.0
DefaultDirName={autopf}\InstrumentTracker
DefaultGroupName=InstrumentTracker
OutputBaseFilename=InstrumentTracker_Setup
Compression=lzma2
SolidCompression=yes

[Files]
Source: "dist\InstrumentTracker\*"; DestDir: "{app}"; Flags: recursesubdirs

[Icons]
Name: "{group}\InstrumentTracker"; Filename: "{app}\InstrumentTracker.exe"
Name: "{autodesktop}\InstrumentTracker"; Filename: "{app}\InstrumentTracker.exe"

[Run]
Filename: "{app}\InstrumentTracker.exe"; Description: "Запустить InstrumentTracker"; Flags: postinstall nowait skipifsilent
```

## � Тестовый режим с логами (Debug)

### Способ 1: Запуск через командную строку (рекомендуется)

Запустите приложение через `cmd` или `PowerShell` чтобы видеть все логи:

**Через CMD:**
```cmd
cd dist\InstrumentTracker
InstrumentTracker.exe
```

**Через PowerShell:**
```powershell
cd dist\InstrumentTracker
.\InstrumentTracker.exe
```

В терминале будут отображаться все `print()` сообщения, включая:
- Инициализация базы данных
- Загрузка данных
- Ошибки подключения
- Статус операций

### Способ 2: Сборка с консолью для отладки

Для постоянного отображения консоли измените в `InstrumentTracker_Windows.spec`:

```python
exe = EXE(
    ...
    console=True,  # Изменить с False на True
    ...
)
```

Затем пересоберите:
```cmd
pyinstaller InstrumentTracker_Windows.spec --clean -y
```

Теперь при запуске всегда будет открываться консоль с логами.

### Способ 3: Перенаправление вывода в файл

Чтобы сохранить все логи в файл:

```cmd
cd dist\InstrumentTracker
InstrumentTracker.exe > logs.txt 2>&1
```

Затем откройте `logs.txt` в любом текстовом редакторе.

### Полезные команды для диагностики

**Проверка версий:**
```cmd
python --version
pip show PyQt6
```

**Проверка зависимостей в собранном приложении:**
```cmd
cd dist\InstrumentTracker
dir /b *.dll
```

**Проверка базы данных:**
```cmd
# Перейти в папку с БД
cd %APPDATA%\InstrumentTracker

# Проверить наличие файла
dir inventory.db

# Открыть БД через SQLite (если установлен)
sqlite3 inventory.db "SELECT COUNT(*) FROM Employees;"
```

## 🐛 Устранение неполадок

### Проблема: "VCRUNTIME140.dll отсутствует"

**Решение:** Установите Microsoft Visual C++ Redistributable:
- Скачайте с: https://aka.ms/vs/17/release/vc_redist.x64.exe
- Установите и перезагрузите компьютер

### Проблема: Приложение не запускается без ошибок

**Решение:** Запустите через командную строку чтобы увидеть ошибки:

```cmd
cd dist\InstrumentTracker
InstrumentTracker.exe
```

Или с полными логами:
```cmd
cd dist\InstrumentTracker
InstrumentTracker.exe > debug.txt 2>&1
notepad debug.txt
```

### Проблема: "Не удалось подключиться к базе данных"

**Решение:** Удалите старую БД и перезапустите:

```cmd
rmdir /s /q %APPDATA%\InstrumentTracker
```

### Проблема: PyInstaller выдает ошибку при сборке

**Решение 1:** Очистите кэш и пересоберите:

```cmd
pyinstaller InstrumentTracker_Windows.spec --clean -y
```

**Решение 2:** Удалите папки build и dist:

```cmd
rmdir /s /q build dist
pyinstaller InstrumentTracker_Windows.spec -y
```

## 📝 Проверочный список перед распространением

- [ ] Приложение запускается без ошибок
- [ ] Все функции работают (вход, добавление активов, выдача/возврат)
- [ ] Горячие клавиши работают
- [ ] База данных создается автоматически
- [ ] Тестовые данные загружаются при первом запуске
- [ ] Иконки отображаются корректно
- [ ] Темы (светлая/темная) переключаются
- [ ] Email уведомления настраиваются (если используются)

## 🆘 Поддержка

При проблемах со сборкой:
1. Убедитесь, что Python 3.11+
2. Проверьте версию PyQt6: `pip show PyQt6`
3. Убедитесь, что все зависимости установлены
4. Проверьте что `inventory.db` существует в корне проекта

## 🎉 Готово!

После успешной сборки у вас будет полностью автономное Windows приложение, готовое к распространению!
