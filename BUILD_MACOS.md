# 🍎 Инструкция по сборке InstrumentTracker для macOS

## Требования

- **macOS 11+** (Big Sur или новее)
- **Python 3.11+**
- **Xcode Command Line Tools** (для некоторых зависимостей)

## Быстрая сборка

### 1. Установите зависимости

```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Или создайте новое
python3 -m venv .venv
source .venv/bin/activate

# Установите пакеты
pip install PyQt6 openpyxl pillow pyinstaller
```

### 2. Соберите приложение

```bash
pyinstaller InstrumentTracker.spec --clean -y
```

### 3. Запустите приложение

```bash
open dist/InstrumentTracker.app
```

Или двойной клик в Finder на `dist/InstrumentTracker.app`.

## Расположение данных

База данных хранится в:
```
~/Library/Application Support/InstrumentTracker/inventory.db
```

Для доступа через Finder:
1. `Cmd + Shift + G`
2. Введите: `~/Library/Application Support/InstrumentTracker`
3. Нажмите Go

## Удаление старой БД (при необходимости)

```bash
rm -rf ~/Library/Application\ Support/InstrumentTracker
```

## Распространение

Для передачи приложения другим пользователям:

1. Скопируйте `InstrumentTracker.app` из папки `dist`
2. Создайте ZIP архив:
   ```bash
   cd dist
   zip -r InstrumentTracker.zip InstrumentTracker.app
   ```
3. Передайте ZIP файл

**Примечание:** Получатели могут увидеть предупреждение "От неизвестного разработчика". Чтобы открыть:
- Правый клик → "Открыть"
- Или: Системные настройки → Безопасность → "Открыть в любом случае"

## Создание DMG образа (опционально)

Для профессионального распространения можно создать .dmg:

```bash
# Установите create-dmg
brew install create-dmg

# Создайте DMG
create-dmg \
  --volname "InstrumentTracker" \
  --window-pos 200 120 \
  --window-size 800 400 \
  --icon-size 100 \
  --icon "InstrumentTracker.app" 200 190 \
  --app-drop-link 600 185 \
  "InstrumentTracker.dmg" \
  "dist/InstrumentTracker.app"
```

## Устранение неполадок

### Ошибка: "Не удалось подключиться к базе данных"

Удалите старую БД:
```bash
rm -rf ~/Library/Application\ Support/InstrumentTracker
```

### Ошибка при сборке

Очистите и пересоберите:
```bash
pyinstaller InstrumentTracker.spec --clean -y
```

## Готово!

Приложение готово к использованию и распространению! 🎉
